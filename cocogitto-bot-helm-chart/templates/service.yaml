apiVersion: v1
kind: Service
metadata:
  name: {{ include "cocogitto-bot-helm-chart.fullname" . }}
  labels:
    {{- include "cocogitto-bot-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cocogitto-bot-helm-chart.selectorLabels" . | nindent 4 }}
